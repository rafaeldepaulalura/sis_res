# ==========================================================================
# Variáveis lidas pelo docker-compose (Postgres). Copie para .env na raiz.
# As variáveis do BACKEND ficam em backend/.env (ver backend/.env.example).
# ==========================================================================
POSTGRES_USER=restaurante
POSTGRES_PASSWORD=restaurante
POSTGRES_DB=restaurante_saas
POSTGRES_PORT=5432
